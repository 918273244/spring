1、首先自定义一个集合容器，将session存储到集合容器中，便于管理

2、为了解决该容器的并发访问问题，使用Collections.synchronizedList()来生成容器

3、将session添加到集合中

4、自定义一个定时器，用来定时扫描集合中的session

5、为了在定时器中获取该容器，需要通过构造函数将该容器传进对象中

6、遍历session容器，并进行增删操作。为了解决并发异常问题，使用list.listIterator()方法获取iterator，从而遍历容器，并进行增删操作

7、为了解决容器的并发增删操作，需要同同步代码块来解决。但是这两部分的代码不在一起，因此使用同步代码块中的锁旗标lock来解决问题，只要synchronized(lock){}中的lock相同即可

8、